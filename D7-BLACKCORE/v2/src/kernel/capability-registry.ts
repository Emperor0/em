import type { CapabilityRecord, CapabilityStatus, Id } from "./types.js";
import { clamp, nowIso } from "./utils.js";
export class CapabilityRegistry { #items=new Map<string,CapabilityRecord>(); upsert(id:string,status:CapabilityStatus,score:number,evidenceIds:Id[]=[],notes?:string):CapabilityRecord{const record:CapabilityRecord={id,status,score:clamp(score,0,100),evidenceIds:[...evidenceIds],...(status==="verified"?{lastVerifiedAt:nowIso()}:{}),...(notes?{notes}:{})};this.#items.set(id,record);return record;} get(id:string):CapabilityRecord|undefined{return this.#items.get(id);} all():CapabilityRecord[]{return[...this.#items.values()].sort((a,b)=>a.id.localeCompare(b.id));} isUsable(id:string,minScore=70):boolean{const item=this.#items.get(id);return!!item&&(item.status==="verified"||item.status==="partial")&&item.score>=minScore;} }
