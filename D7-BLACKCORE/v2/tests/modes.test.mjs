import test from"node:test";import assert from"node:assert/strict";import{opportunityScore,shouldValidateBeforeBuild}from"../dist/src/modes/money-mode.js";import{DeviceMode}from"../dist/src/modes/device-mode.js";import{BuilderStudio}from"../dist/src/builder/builder-studio.js";test("opportunity score rewards real demand",()=>{const high=opportunityScore({id:"1",title:"x",buyerEvidence:100,paymentLikelihood:90,deliveryFit:90,competition:20,speedToCash:90,reachability:90}),low=opportunityScore({id:"2",title:"y",buyerEvidence:20,paymentLikelihood:20,deliveryFit:30,competition:90,speedToCash:20,reachability:20});assert.ok(high>low)});test("weak demand requires validation",()=>assert.equal(shouldValidateBeforeBuild({id:"1",title:"x",buyerEvidence:50,paymentLikelihood:90,deliveryFit:90,competition:20,speedToCash:90,reachability:90}),true));test("strong opportunity can proceed",()=>assert.equal(shouldValidateBeforeBuild({id:"1",title:"x",buyerEvidence:90,paymentLikelihood:80,deliveryFit:90,competition:20,speedToCash:90,reachability:80}),false));test("device adapter snapshot validates",()=>{const d=new DeviceMode({snapshot:()=>({platform:"win32",release:"x",arch:"x64",hostname:"pc",cpuModel:"cpu",logicalCpus:12,totalMemoryBytes:16,freeMemoryBytes:8,uptimeSeconds:100})});assert.equal(d.snapshot().platform,"win32")});test("device adapter rejects invalid cpu count",()=>{const d=new DeviceMode({snapshot:()=>({platform:"x",release:"x",arch:"x",hostname:"x",cpuModel:"x",logicalCpus:0,totalMemoryBytes:0,freeMemoryBytes:0,uptimeSeconds:0})});assert.throws(()=>d.snapshot(),/INVALID_DEVICE_SNAPSHOT/)});test("builder plan includes real install gate",()=>{const b=new BuilderStudio(),p=b.makePlan({goal:"app",kind:"desktop",constraints:[],qualityTarget:95});assert.ok(p.acceptanceGates.some(x=>x.includes("install/run")))});test("builder ranks stronger stack first",()=>{const b=new BuilderStudio(),ranked=b.rankStacks([{name:"A",fit:90,runtimeSafety:90,performance:90,ecosystem:90,maintainability:90,reasons:[]},{name:"B",fit:50,runtimeSafety:50,performance:50,ecosystem:50,maintainability:50,reasons:[]}]);assert.equal(ranked[0]?.name,"A")});
